# Replaces the welcome page shipped in the nginx image. Requests that match no
# configured server_name - bare IP scans, mostly - are dropped without a reply.
server {
    listen 80 default_server;
    listen [::]:80 default_server;
    server_name _;
    return 444;
}
